- content_for :title do
  Employer Home
  
= render 'layouts/flash', flash: flash

.filters.left
  .filter-title Filter Bids
  %ul
    = filter_link 'All', employers_root_path, nil
    = filter_link 'Sent', employers_root_path, 'sent'
    = filter_link 'Accepted', employers_root_path, 'accepted'
    = filter_link 'Declined', employers_root_path, 'declined'

.bids.right
  - unless @bids.count > 0
    .no-bids There are no bids to display
  = render @bids