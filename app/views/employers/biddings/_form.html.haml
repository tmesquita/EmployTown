= form_for [:employers, @bidding], :url => {:action => 'create', :seeker_id => params[:seeker_id]} do |f|  
  - if @bidding.errors.any?  
    .error
      = image_tag 'Exclamation.png'
      %strong &nbsp;Please fix the following errors
      %br
      %br
        - @bidding.errors.full_messages.each do |msg|  
          %li.message= "&nbsp;#{msg}"

  .input-container
    .left
      .input
        = f.label :title, "Job Title"
        %br
        = f.text_field :title
      .input
        = f.label :comment, "Description"
        %br
        = f.text_area :comment, :size => "30x8" 
      %input{:type => 'hidden', :name => 'seeker_id', :value => @seeker_id}
      .submit
        = f.submit 'Submit Bid' 
    .right
      .input
        = f.label :salary
        %br
        = f.text_field :salary
      .input
        = f.label :salary_term
        %br
        = f.select :salary_term, options_for_select([['Year', 'year'], ['Hour', 'hour']])
      .input
        = f.label :contact_email_address
        %br
        = f.text_field :contact_email
    .clear
  
    

