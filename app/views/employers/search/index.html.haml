%h1 Search for potential employees
=render 'form'

-#unless params[:search].blank?
  -#unless @users.count == 0
    -# for user in @users
      -#%p User Name
      -#user.first_name
      -#user.last_name
-unless params[:search].blank?
  %h2 TESTING