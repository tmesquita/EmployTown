.options
  - if !@seeker.has_resume?
    This User has not Uploaded a resume
  -else
    = link_to "View My Resume", @seeker.resume.url
  - if !@seeker.has_bid_from_employer?(current_user)
    = link_to 'Send Bid', new_employers_bidding_path(:seeker_id => @seeker.id)

%h2= @seeker.to_s + "'s Profile"

%p
  = image_tag @seeker.photo.url(:regular)

%p
  About Me:
  = simple_format @seeker.about_me