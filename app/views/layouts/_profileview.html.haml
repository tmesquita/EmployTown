%h2= @seeker.to_s + "'s Profile"

%p
  = image_tag @seeker.photo.url(:regular)
  
%p
- if @seeker.resume_file_name.eql? nil
  This User has not Uploaded a resume
-else
  %p
    = link_to "View My Resume", @seeker.resume.url